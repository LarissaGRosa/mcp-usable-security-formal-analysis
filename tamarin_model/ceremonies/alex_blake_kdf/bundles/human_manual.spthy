/*
  bundles/human_manual.spthy -- the human layer WITHOUT a demand profile: mask gates + one-answer +
  outcome matrix + Alex enable. Profiles that SWAP the lexicon for an interface / population seed
  (P7 hardened, P9 expert, P10/P11 profile-local seeds) include THIS + their own demand rows.
  bundles/human_common.spthy = this + the baseline lexicon_tlx.
*/
#include "../../../core/transitions/mask_state.spthy"
#include "../../../core/transitions/answered_once.spthy"
#include "../../../core/masks/outcome_policy.spthy"
#include "../protocol/stress_alex.spthy"
