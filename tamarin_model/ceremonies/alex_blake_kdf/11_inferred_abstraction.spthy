theory ToyCeremony_InferredAbstraction
begin

/*
  ENTRY POINT -- Experiment 11 · sigma6 Abstraction INFERRED from the operation.
  A minimal ceremony where Alex is posed a 'verify' operation over an opaque fingerprint; the
  stressor is inferred from the operation tag by core/stressors/abstraction_inferred.spthy
  -- no designer flag. Naive (so set) then accepts a tampered fingerprint (a mistake).
  Self-contained. Run: check.py --prove ceremonies/alex_blake_kdf/11_inferred_abstraction.spthy
*/

#include "../../core/types.spthy"

// -- human layer (inferred stressor + verify-operation f_H) --
#include "../../core/stressors/abstraction_inferred.spthy"
#include "../../core/masks/attentive_verify_op.spthy"
#include "../../core/masks/naive_verify_op.spthy"
#include "../../core/transitions/mask_state.spthy"
#include "../../core/transitions/answered_once.spthy"

// -- protocol: init (Alex) + the verify-posing step --
#include "protocol/init.spthy"
#include "protocol/verify_op.spthy"

// -- experiment --
#include "experiments/11_inferred_abstraction.spthy"

end
