theory ToyCeremony_BusyUnderLoad
begin

/*
  ENTRY POINT -- Experiment 01 · Busy under load (one stressor + one transition).
  Profile (machinery §6.1): E_0 = < C0, {Attentive, Busy}, {sigma1 HighCognitiveLoad} >.
  = base spine + Busy-on-CALC_SK machinery + request/ack Msg3 + L1 lemmas.
  Run: check.py --prove ceremonies/alex_blake_kdf/01_busy_under_load.spthy
*/

#include "ceremony.base.spthy"

// -- human layer (the delta this experiment adds) --
#include "../../core/masks/attentive_calc.spthy"
#include "../../core/masks/busy_calc.spthy"
#include "../../core/stressors/cognitive_load.spthy"
#include "../../core/transitions/from_attentive.spthy"

// -- Msg3 variant --
#include "protocol/msg3_request.spthy"

// -- experiment --
#include "experiments/01_busy_under_load.spthy"

end
