theory ToyCeremony_P0_Baseline
begin

/*
  P0 -- BASELINE: the layered pipeline, no stressor. Both parties derive SK = kdf(N_A,N_B)
  Attentively; the ceremony finishes and the keys agree. Run:
  check.py --prove ceremonies/alex_blake_kdf/P0_baseline.spthy
*/

#include "spine.spthy"                    // crypto + KDF wire + Alex compute pipeline
#include "protocol/finish.spthy"          // plain finish + session
#include "interface/recipient.spthy"      // Blake computes too (P0_agreement checks both keys)
#include "bundles/human_common.spthy"

#include "experiments/P0_baseline.spthy"

end
