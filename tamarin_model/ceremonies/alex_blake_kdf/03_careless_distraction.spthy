theory ToyCeremony_CarelessDistraction
begin

/*
  ENTRY POINT -- Experiment 03 · Careless (distraction / safe-fail).
  Profile (machinery §6.1): E_3 = < C0, {Attentive, Careless}, {sigma2 ExternalDistraction} >.
  = base spine + Careless-on-CALC_SK machinery + request/ack Msg3 + L3 lemmas.
  Reuses Experiment 01's request flow; swaps stressor/mask/outcome (timeout = safe-fail).
  Run: check.py --prove ceremonies/alex_blake_kdf/03_careless_distraction.spthy
*/

#include "ceremony.base.spthy"

// -- human layer (the delta this experiment adds) --
#include "../../core/masks/attentive_calc.spthy"
#include "../../core/masks/careless_calc.spthy"
#include "../../core/stressors/external_distraction.spthy"
#include "../../core/transitions/mask_state.spthy"
#include "../../core/transitions/answered_once.spthy"

// -- Msg3 variant --
#include "protocol/msg3_request.spthy"

// -- experiment --
#include "experiments/03_careless_distraction.spthy"

end
