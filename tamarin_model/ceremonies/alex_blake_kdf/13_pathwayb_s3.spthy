theory ToyCeremony_PathwayB_S3
begin

/*
  ENTRY POINT -- Experiment 13 · Pathway B (sigma4 MisleadingTerminology, AWS-S3 mistake).
  The second failure pathway: NO stressor/SetMask -- the user stays Attentive throughout, yet a
  SET_POLICY step with misleading terminology makes them set an unsafe (public) policy. The mistake
  is read directly from the request's terminology flag in f_H (core/masks/attentive_policy.spthy).
  Run: check.py --prove ceremonies/alex_blake_kdf/13_pathwayb_s3.spthy
*/

#include "../../core/types.spthy"

// -- human layer: Attentive SET_POLICY f_H (Pathway-B mistake on misleading terminology) --
#include "../../core/masks/attentive_policy.spthy"
#include "../../core/transitions/mask_state.spthy"
#include "../../core/transitions/answered_once.spthy"

// -- protocol: init + the SET_POLICY step (terminology = the minimal supplied info) --
#include "protocol/init.spthy"
#include "protocol/policy_ui.spthy"

// -- experiment --
#include "experiments/13_pathwayb_s3.spthy"

end
