theory ToyCeremony_AnxietyFearful
begin

/*
  ENTRY POINT -- Experiment 07 · SecurityAnxiety -> Fearful -> abort.
  Profile (machinery §6.1): E_7 = < C0 + AUTHORIZE phase, {Attentive, Fearful}, {sigma5 SecurityAnxiety} >.
  KDF baseline inline (Attentive), then an AUTHORIZE phase where a sensitive request provokes
  anxiety, driving Attentive -> Fearful, who aborts (withdrawal / safe-fail).
  Run: check.py --prove ceremonies/alex_blake_kdf/07_anxiety_fearful.spthy
*/

#include "ceremony.base.spthy"

// -- human layer --
#include "../../core/masks/attentive_authorize.spthy"
#include "../../core/masks/fearful_authorize.spthy"
#include "../../core/stressors/security_anxiety.spthy"
#include "../../core/transitions/mask_state.spthy"
#include "../../core/transitions/answered_once.spthy"

// -- protocol: inline Msg3 (KDF baseline) + session + the AUTHORIZE phase --
#include "protocol/msg3_inline.spthy"
#include "protocol/session.spthy"
#include "protocol/authorize_ui.spthy"

// -- experiment --
#include "experiments/07_anxiety_fearful.spthy"

end
