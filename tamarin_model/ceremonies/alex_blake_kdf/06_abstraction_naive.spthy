theory ToyCeremony_AbstractionNaive
begin

/*
  ENTRY POINT -- Experiment 06 · Abstraction -> Naive -> mistake.
  Profile (machinery §6.1): E_6 = < C0 + VERIFY_KEY phase, {Attentive, Naive}, {sigma6 Abstraction} >.
  KDF baseline inline (Attentive), then a VERIFY_KEY fingerprint-check phase where the abstract
  crypto drives Attentive -> Naive, who accepts a tampered fingerprint (a Norman mistake).
  Run: check.py --prove ceremonies/alex_blake_kdf/06_abstraction_naive.spthy
*/

#include "ceremony.base.spthy"

// -- human layer --
#include "../../core/masks/attentive_verify.spthy"
#include "../../core/masks/naive_verify.spthy"
#include "../../core/stressors/abstraction.spthy"
#include "../../core/transitions/mask_state.spthy"
#include "../../core/transitions/answered_once.spthy"

// -- protocol: inline Msg3 (KDF baseline) + session + the VERIFY_KEY phase --
#include "protocol/msg3_inline.spthy"
#include "protocol/session.spthy"
#include "protocol/verify_ui.spthy"

// -- experiment --
#include "experiments/06_abstraction_naive.spthy"

end
