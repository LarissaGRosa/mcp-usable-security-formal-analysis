theory SecureEmail_S1_TrioPGP
begin

/*
  S1 -- TRIO on the PGP key-verification surface: sigma6 Abstraction (Naive), sigma8 Habituation
  (click-through), sigma2 Concurrency (skip-the-check). All three converge on the same weak point --
  the DY-substituted keyserver answer survives a degraded compare and the message is encrypted to the
  adversary. Run: check.py --prove ceremonies/secure_email/S1_trio_pgp.spthy
*/

#include "spine.spthy"

// -- the trio --
#include "../../core/stressors/abstraction.spthy"
#include "../../core/stressors/habituation.spthy"
#include "../../core/stressors/distraction_concurrent.spthy"
// -- opt-in outcome rows this trio's stories need --
#include "../../core/masks/outcome_habituated_clickthrough.spthy"
#include "../../core/masks/outcome_careless_skipcheck.spthy"

#include "experiments/S1_trio_pgp.spthy"

end
