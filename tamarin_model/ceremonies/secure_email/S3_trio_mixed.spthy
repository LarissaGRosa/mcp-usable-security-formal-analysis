theory SecureEmail_S3_TrioMixed
begin

/*
  S3 -- MIXED trio spanning both pathways + the safe-fail: sigma6 Abstraction (PGP compare),
  sigma3 Deadline (password pane), SecurityAnxiety (send authorize). One failure family per pathway
  plus the one degradation that HELPS -- the Fearful sender freezes and the message never leaves.
  Run: check.py --prove ceremonies/secure_email/S3_trio_mixed.spthy
*/

#include "spine.spthy"

// -- the trio --
#include "../../core/stressors/abstraction.spthy"
#include "../../core/stressors/time_pressure_deadline.spthy"
#include "../../core/stressors/security_anxiety.spthy"

#include "experiments/S3_trio_mixed.spthy"

end
